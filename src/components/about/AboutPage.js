import React from "react";

const AboutPage = () => (
    <div>
        <h2>About</h2>
        <p>
            this app use React, Redux and React Router
        </p>
    </div>
);

export default AboutPage;